export interface MenuItems {
    menuItemId: string;
    menuId: string;
    name: string;
    price: number;
    description: string;
    imageUrl?: string; // Opsiyonel çünkü bazıları dönmüyor
  }
  